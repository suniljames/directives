# <Project Name> — Credentials & Programmatic Access

<!-- Fill in and keep in the project repo. NEVER put credential VALUES in this
     file — it routes agents to the right access method. Values live in the
     shell environment, a secret manager, or the harness's tool-server config
     (example: Claude Code MCP config). -->

## Quick decision guide

| I need to… | Use |
|------------|-----|
| <e.g. query the production DB read-only> | <method> |
| <e.g. deploy / read deploy logs> | <method> |

## Which services need secrets at all

- **No local secret needed:** <services reachable via an authenticated CLI or the harness's tool servers>
- **Secret required:** <services needing env vars — name the VARIABLE, not the value>
- **If a required secret is missing:** <where to get it / who to ask> — say so and stop; do not improvise an alternative path.

## Per-service access ladders

Give each service a numbered primary → fallback ladder so an agent degrades
deterministically instead of improvising:

### <Service name>
1. **Primary:** <method>
2. **Fallback:** <method — when primary fails how?>
3. **Last resort:** <method — requires confirmation?>

## Rules

- Never echo a secret's value into chat, logs, or issue comments — probe presence with a boolean check, never by printing.
- Never commit secrets; if one lands in history, rotate first, then scrub.
- Prefer read-only credentials for read-only tasks.
